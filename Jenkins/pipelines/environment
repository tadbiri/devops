pipeline {
    agent any

    environment {
        NAME = 'mohamad'
        LASTNAME = 'tadbiri'
    }

    stages {
        stage('Build') {
            steps {
                sh 'echo $NAME $LASTNAME'
            }
        }
    }
}
